/**
 * Player configuration and settings
 */

export const PLAYER_CONFIG = {
  x: 400, // Default spawn x
  y: 300, // Default spawn y
  speed: 150, // Pixels per second
  interactionRadius: 80, // Distance to interact with NPCs
  wobbleAmplitude: 2, // Idle wobble rotation amplitude (degrees)
  wobbleSpeed: 0.002 // Idle wobble animation speed
};
